//game related constants
const LEFT = Symbol('left');
const RIGHT = Symbol('right');
const UP = Symbol('up');
const DOWN = Symbol('down');
const GAME_AREA_WIDTH = 35;
const GAME_AREA_HEIGHT = 20;
const GAME_WON = Symbol('won');
const GAME_LOST = Symbol('lost');
const WIN_SCORE = 5;
const GAME_SPEED = 120;
export { LEFT, RIGHT, UP, DOWN, GAME_AREA_HEIGHT, GAME_AREA_WIDTH, GAME_WON, GAME_LOST, WIN_SCORE, GAME_SPEED };
